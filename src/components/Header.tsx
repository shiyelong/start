/**
 * Re-export the canonical Header from layout/Header.tsx
 * This file exists for backward compatibility — many pages import from '@/components/Header'
 */
export { default } from '@/components/layout/Header';
